flask
flask-cors
requests
psutil

# Поддержка Discord-бота (раздел «Discord» в настройках) требует discord.py.
# Он не в основном списке нарочно — если его не установить, весь сервер всё
# равно запустится, просто раздел Discord покажет, что библиотеки не хватает
# (см. p20_discord.py: DISCORD_PY_AVAILABLE). Установи отдельно, если нужен бот:
#   pip install discord.py
# discord.py

# ЗАПУСК локальных (GGUF) моделей (не только скачивание) требует ещё
# llama-cpp-python. Он не в этом файле нарочно — это тяжёлая компилируемая
# зависимость (нужен C++ компилятор, а под GPU — свои флаги сборки), и она
# не должна ронять весь `pip install -r requirements.txt`, если её сборка
# не пройдёт. Ставь отдельно, см. раздел «Локальные модели» в README.md.
# Вместо llama-cpp-python можно поставить бинарник llama-server
# (в Termux: pkg install llama-cpp) — проект найдёт и запустит его сам.
